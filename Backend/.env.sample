# MySQL (Workbench is just the GUI — these values must match your MySQL server)
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=sandy0318
DB_NAME=lab1

# Server
PORT=4000
SESSION_SECRET=lab1secret
CLIENT_ORIGIN=http://localhost:3000

# AWS S3
AWS_REGION=us-east-2
AWS_ACCESS_KEY_ID=  # change to your own IAM access key
AWS_SECRET_ACCESS_KEY=  # change to your own IAM secret access key
S3_BUCKET=lab1-host-images
S3_PUBLIC_BASE=https://lab1-host-images.s3.us-east-2.amazonaws.com
