{
  "name": "data236-lab1",
  "version": "1.0.0",
  "private": true,
  "scripts": {
  "dev": "npm --prefix Frontend start",
  "dev:frontend": "npm --prefix Frontend start",
  "dev:backend": "npm --prefix Backend start",
  "dev:agent": "python -m uvicorn agentic.agent.main:app --host 127.0.0.1 --port 8001 --reload",
  "dev:all": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\" \"npm run dev:agent\"",
  "start": "npm run dev"
  }
  ,
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
